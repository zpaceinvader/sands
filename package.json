{
  "name": "planck",
  "version": "1.0.0",
  "proxy": "http://sands.site",
  "private": true,
  "entry": {
    "main": [
      "./src/main.js",
      "./src/main.scss"
    ]
  },
  "scripts": {
    "start": "heisenberg-scripts start",
    "build": "heisenberg-scripts build",
    "test": "heisenberg-scripts test --env=jsdom"
  },
  "browserslist": [
    "> 1%",
    "Edge >= 12",
    "Explorer >= 10",
    "iOS >= 9",
    "Android >= 4.4",
    "last 2 versions"
  ],
  "engines": {
    "node": ">=8"
  },
  "devDependencies": {
    "heisenberg-scripts": "0.2.1"
  },
  "dependencies": {
    "babel-preset-react": "^6.24.1",
    "browserify-fs": "^1.0.0"
  }
}
